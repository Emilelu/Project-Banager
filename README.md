# 📺 PBM - Project Bangumi Manager

> 「PBM, started from 2020.2.」

---

## 📖 项目简介

**PBM（Project Bangumi Manager）** 是一款个人追番管理工具，旨在帮助动漫爱好者高效管理自己的追番、等番和已看历史。项目起源于 2020 年 2 月，最初以 Excel 表格的形式存在，经过多年实际使用中的不断打磨与功能迭代，现已进化为基于 Web 的全栈应用。

### ✨ 设计理念

- **周历视图**：追番以周为单位排列，一目了然今天该追什么
- **状态流转**：等番 → 追番 → 已看，番剧生命周期全覆盖
- **Excel 兼容**：支持从历史 Excel 数据一键导入，无缝迁移
- **轻量部署**：单机部署，数据本地存储，无需云服务

### 🛠 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端框架** | Vue 3 | 渐进式 JavaScript 框架，Composition API |
| **路由** | Vue Router 4 | 官方路由管理，支持页面切换动画 |
| **样式** | TailwindCSS 3 | 实用优先的 CSS 框架，自定义主题色 |
| **HTTP** | Axios | 前端 HTTP 客户端 |
| **构建** | Vite 6 | 下一代前端构建工具，极速 HMR |
| **后端框架** | Express 4 | Node.js Web 应用框架 |
| **数据库** | SQLite (sql.js) | 纯 WASM 实现，无需原生编译，数据本地存储 |
| **Excel 解析** | xlsx | 支持 .xlsx / .xls 文件解析与导入 |
| **文件上传** | Multer | Express 文件上传中间件 |

---

## 🎯 全功能简介

### 📺 正在追番

- **周历视图**：7 列布局，按星期一至星期日排列，直观展示每日更新
- **本月等番提醒**：自动检测等番中本月即将更新的番剧，顶部横幅提醒
- **集数管理**：一键 +1 / -1 集，也可双击直接编辑集数
- **快速链接**：鼠标中键点击卡片任意位置，新标签页打开番剧链接；无链接时自动搜索 bgm.tv
- **右键菜单**：右键弹出快捷操作菜单
- **批量操作**：批量选择、批量删除、全选/取消全选
- **行内编辑**：双击番剧名称、集数、时间、备注等字段直接编辑
- **状态迁移**：移至等番 / 移至已看（需选择观看日期）
- **清空列表**：一键清空所有追番数据

### ⏳ 等待更新

- **列表视图**：表格形式展示所有等番，支持分页
- **新番日历链接**：自动生成当季新番日历链接（bgm.tv），快速查看新番信息
- **批量添加**：支持一次性输入多个番剧名称批量添加
- **快速链接**：鼠标中键点击行任意位置，新标签页打开链接
- **右键菜单**：右键弹出快捷操作菜单
- **批量操作**：批量选择、批量删除、全选/取消全选
- **行内编辑**：双击名称、预计日期、链接、备注等字段直接编辑
- **状态迁移**：移至追番（需选择星期和时间）
- **清空列表**：一键清空所有等番数据

### 📚 已看历史

- **年份分组**：按年份分类浏览已看作品，支持自定义年份标签（如 "2014~2017"）
- **搜索功能**：实时搜索作品名称，快速定位
- **统计信息**：显示本年观看数和总计观看数
- **快速链接**：鼠标中键点击行任意位置，新标签页打开链接
- **批量操作**：批量选择、批量删除、全选/取消全选
- **行内编辑**：双击名称、观看日期、链接、备注等字段直接编辑
- **年份管理**：添加年份、编辑年份标签、删除年份
- **分页浏览**：大量数据时自动分页，可自定义每页条数
- **清空列表**：一键清空所有已看数据

### 📥 导入 Excel

- **智能识别**：自动识别工作表类型（追番/等番/已看），无需手动指定
- **周历解析**：追番表支持 Excel 周历布局，自动识别星期列和更新时间
- **超链接导入**：自动提取 Excel 单元格中的超链接
- **日期格式**：支持多种日期格式（年份、年月、完整日期、年份范围）
- **合并单元格**：已看历史表支持合并单元格年份
- **导入日志**：实时显示导入进度和结果，包含错误和警告信息
- **追加模式**：导入数据追加到现有记录，不覆盖

### 🎨 全局特性

- **主题切换**：日间 / 暗黑 / 跟随系统三种模式
- **实时时钟**：顶部栏显示当前日期时间
- **页面动画**：路由切换动画、按钮交互反馈
- **毛玻璃效果**：现代化 UI 设计，毛玻璃卡片风格
- **响应式布局**：适配不同屏幕尺寸

---

## 📋 详细使用方法

### 安装与启动

#### 前置要求

- **Node.js** >= 18
- **pnpm**（推荐）或 npm

#### 1. 克隆项目

```bash
git clone <repository-url>
cd bangumi-manager
```

#### 2. 安装后端依赖并启动

```bash
cd server
pnpm install
pnpm dev
```

后端服务运行在 `http://localhost:3001`

#### 3. 安装前端依赖并启动

```bash
cd client
pnpm install
pnpm dev
```

前端开发服务器运行在 `http://localhost:5173`

#### 4. 访问应用

打开浏览器访问 `http://localhost:5173`

### 生产构建

```bash
cd client
pnpm build
```

构建产物在 `client/dist/` 目录，可配合 Nginx 等部署。

---

### 📺 正在追番 - 使用指南

1. **添加番剧**：点击「✨ 添加番剧」按钮，填写番剧名称、更新星期、更新时间等信息
2. **选择番剧**：单击卡片选中，选中后可进行编辑、集数调整、状态迁移等操作
3. **调整集数**：
   - 悬浮卡片时出现 +/- 按钮
   - 或使用顶部「➕ +1集」「➖ -1集」按钮
   - 双击集数可直接输入
4. **打开链接**：
   - 点击番剧名称旁的链接图标
   - **鼠标中键**点击卡片任意位置，新标签页打开
   - 无自定义链接时自动搜索 bgm.tv
5. **编辑信息**：双击名称、时间、备注等字段进入行内编辑模式
6. **右键菜单**：右键点击卡片弹出快捷操作菜单
7. **批量操作**：点击「☑️ 批量选择」进入批量模式，勾选后可批量删除
8. **移至等番/已看**：选中番剧后点击对应按钮进行状态迁移

### ⏳ 等待更新 - 使用指南

1. **添加等番**：点击「✨ 添加等番」单个添加，或「📝 批量添加」一次输入多个
2. **查看新番**：点击顶部「📺 当季新番」链接查看 bgm.tv 新番日历
3. **选择番剧**：单击行选中
4. **打开链接**：**鼠标中键**点击行任意位置新标签页打开
5. **编辑信息**：双击字段进入行内编辑
6. **移至追番**：选中后点击「📺 移至追番」，选择更新星期和时间
7. **批量操作**：与追番页操作方式一致

### 📚 已看历史 - 使用指南

1. **浏览记录**：通过年份下拉框选择查看特定年份，或选择「全部年份」
2. **搜索**：在搜索框输入关键词实时过滤
3. **添加记录**：点击「✨ 添加记录」手动添加
4. **年份管理**：
   - 「✨ 添加年份」创建新的年份分组
   - 「✏️ 编辑年份」修改年份标签（如改为 "2014~2017"）
   - 「🗑️ 删除年份」删除年份分组（不删除作品记录）
5. **打开链接**：**鼠标中键**点击行任意位置新标签页打开
6. **编辑信息**：双击字段进入行内编辑
7. **批量操作**：与追番页操作方式一致

### 📥 导入 Excel - 使用指南

1. **准备 Excel 文件**：确保文件包含以下工作表之一：
   - 含「正在」或「追番」关键词的工作表 → 追番数据
   - 含「等待」或「等番」关键词的工作表 → 等番数据
   - 含「已看」或「历史」关键词的工作表 → 已看数据
2. **选择文件**：点击「🔍 浏览文件」选择 Excel 文件
3. **开始导入**：点击「▶️ 开始导入」，观察导入日志
4. **查看结果**：导入完成后日志显示成功数量和错误信息

#### Excel 格式要求

**追番表（周历布局）**：
- 第一行为星期表头（如 Monday、周二等）
- 每行第一列为更新时间（Excel 小数格式，0.5 = 12:00）
- 番剧名支持「名称 集数」格式自动提取集数
- 单元格超链接自动导入

**等番表**：
- 每行一条：番剧名称 | 预计日期 | 备注
- 日期支持：2025/07、2025/07/15、2025/07/15 14:30

**已看历史表**：
- 支持合并单元格年份（如 2014~2017）
- 作品名支持「名称 (备注)」格式
- 日期支持：年份、年月、完整日期、年份范围

---

## 📁 项目结构

```
bangumi-manager/
├── client/                        # 前端项目
│   ├── index.html                 # HTML 入口
│   ├── package.json               # 前端依赖配置
│   ├── vite.config.js             # Vite 构建配置
│   ├── tailwind.config.js         # TailwindCSS 主题配置
│   ├── postcss.config.js          # PostCSS 配置
│   ├── public/
│   │   ├── favicon.png            # 网站图标
│   │   └── logo.png               # 应用 Logo
│   └── src/
│       ├── main.js                # Vue 应用入口
│       ├── App.vue                # 根组件（侧边栏+路由）
│       ├── style.css              # 全局样式与动画
│       ├── api/
│       │   └── index.js           # API 请求封装
│       ├── composables/
│       │   └── useTheme.js        # 主题切换逻辑
│       ├── router/
│       │   └── index.js           # 路由配置
│       └── views/
│           ├── WatchingView.vue   # 正在追番页面
│           ├── RemainingView.vue  # 等待更新页面
│           ├── WatchedView.vue    # 已看历史页面
│           └── ImportView.vue     # 导入 Excel 页面
│
└── server/                        # 后端项目
    ├── index.js                   # Express 服务器入口
    ├── database.js                # 数据库模块（SQLite CRUD）
    ├── excelImporter.js           # Excel 导入解析模块
    ├── inspect_excel.js           # Excel 调试工具
    ├── bangumi.db                 # SQLite 数据库文件
    ├── package.json               # 后端依赖配置
    └── uploads/                   # 临时上传目录
```

---

## 🔌 API 接口

### 追番

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/watching | 获取所有追番 |
| GET | /api/watching/:day | 按星期获取追番 |
| POST | /api/watching | 添加追番 |
| PUT | /api/watching/:id | 更新追番 |
| DELETE | /api/watching/:id | 删除追剧 |
| POST | /api/watching/:id/increment | +1 集 |
| POST | /api/watching/:id/decrement | -1 集 |
| POST | /api/watching/:id/to-remaining | 移至等番 |
| POST | /api/watching/:id/to-watched | 移至已看 |

### 等番

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/remaining | 获取所有等番 |
| POST | /api/remaining | 添加等番 |
| POST | /api/remaining/batch-add | 批量添加等番 |
| PUT | /api/remaining/:id | 更新等番 |
| DELETE | /api/remaining/:id | 删除等番 |
| POST | /api/remaining/:id/to-watching | 移至追番 |

### 已看

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/watched | 获取所有已看 |
| GET | /api/watched/year/:year | 按年份获取已看 |
| POST | /api/watched | 添加已看 |
| PUT | /api/watched/:id | 更新已看 |
| DELETE | /api/watched/:id | 删除已看 |

### 已看年份

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/watched-years | 获取所有年份 |
| POST | /api/watched-years | 添加年份 |
| PUT | /api/watched-years/:id | 更新年份 |
| DELETE | /api/watched-years/:id | 删除年份 |

### 批量操作

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/watching/batch-delete | 批量删除追番 |
| POST | /api/remaining/batch-delete | 批量删除等番 |
| POST | /api/watched/batch-delete | 批量删除已看 |
| POST | /api/clear/watching | 清空追番 |
| POST | /api/clear/remaining | 清空等番 |
| POST | /api/clear/watched | 清空已看 |
| POST | /api/clear/watched-years | 清空年份 |
| POST | /api/clear/all | 清空所有数据 |

### 导入与统计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/import | 上传并导入 Excel |
| GET | /api/stats | 获取统计数据 |

---

## 🗄️ 数据库表结构

### watching（追番表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| name | TEXT | 番剧名称（唯一） |
| day_of_week | TEXT | 更新星期 |
| time_slot | TEXT | 更新时间 |
| current_episode | TEXT | 当前集数 |
| url | TEXT | 番剧链接 |
| url_params | TEXT | 链接参数 |
| notes | TEXT | 备注 |
| sort_order | REAL | 排序序号 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### remaining（等番表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| name | TEXT | 番剧名称（唯一） |
| expected_date | TEXT | 预计日期 |
| url | TEXT | 番剧链接 |
| url_params | TEXT | 链接参数 |
| notes | TEXT | 备注 |
| sort_order | REAL | 排序序号 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### watched（已看表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| name | TEXT | 作品名称 |
| watch_date | TEXT | 观看日期 |
| url | TEXT | 作品链接 |
| url_params | TEXT | 链接参数 |
| notes | TEXT | 备注 |
| sort_order | REAL | 排序序号 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### watched_years（已看年份表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| year_label | TEXT | 年份标签（唯一，如 "2025" 或 "2014~2017"） |
| sort_order | REAL | 排序序号 |
| created_at | TIMESTAMP | 创建时间 |

---

## 🚧 TODO

### 🔐 多用户支持
- [ ] 用户注册 / 登录系统
- [ ] 用户数据隔离（每用户独立数据库或行级隔离）
- [ ] 用户权限管理
- [ ] 个人设置与偏好持久化

### 📱 跨平台
- [ ] 移动端适配优化（响应式布局改进）
- [ ] PWA 支持（离线访问、桌面安装）
- [ ] Electron 桌面客户端打包
- [ ] 移动端手势操作支持

### ✨ 细节优化
- [ ] 拖拽排序（追番/等番/已看列表内拖拽调整顺序）
- [ ] 键盘快捷键（全局快捷键支持，如 Ctrl+N 添加）
- [ ] 数据导出（导出为 Excel / JSON）
- [ ] 数据备份与恢复
- [ ] 番剧封面图支持（自动从 bgm.tv 抓取）
- [ ] 追番进度百分比显示
- [ ] 评分系统（为已看作品打分）
- [ ] 标签/分类系统（按类型、标签筛选）
- [ ] 通知提醒（番剧更新提醒）

### 🔧 技术改进
- [ ] WebSocket 实时数据同步
- [ ] 服务端分页与搜索（减少前端数据量）
- [ ] 数据库迁移脚本管理
- [ ] 单元测试与 E2E 测试
- [ ] Docker 容器化部署
- [ ] CI/CD 自动化构建与部署
- [ ] 国际化（i18n）支持

---

## 📄 License

MIT License

---

> 💝 PBM - 从 Excel 到 Web，追番管理从未如此优雅。

